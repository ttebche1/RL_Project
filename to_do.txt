# Analyze velocity during tests - see why the total power used seems smaller
# Compare to magnitude of currents - do we need a 5m/s option for velocity? Will it ever be used?
# Be able to set current and target location for testing
# Merge constant velocity into this file and remove ASV
# Do we need to add acceleration? Is it jumping around different velocities?
#
# Later:
# - Add particle filter for estimation / observations OR add noise to target stuff to simulate a filter
# - Add moving target w/ trailing
# - Add baseline comparison
# - Add 3D environment (depth)
# - Add complex comms things such as doppler
# - Turn it into a whale problem!
# - Add a more complex current model
# - Add complex 3D bathymetry (obstacles)
# - What if the target moves at varying speeds?
#
# Multi-agents:
# - Add multiple agents
# - Remove full observability, but they know each other's distance to target
# - Try no comms
# - Add a larger search space than 2km
# - Try different multi-RL algorithms
#
# Speed:
# - Does prompting it with a non-ML search algorithm help?
# - Curriculum training 
# - Automatically tune hyperparameters
# - Does adding change in distance vectors or magnitude improve speed of AUV script?
